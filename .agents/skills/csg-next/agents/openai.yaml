interface:
  display_name: "Csg Next"
  short_description: "Repo-specific patterns and workflows for csg-next"
  default_prompt: "Use the csg-next repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true