import MoonCalculator from '../../components/MoonCalculator';

export const metadata = {
  title: 'Moon Sign & Phase Calculator • Cosmic Spirit Guide',
  description: 'Discover your natal moon sign and the current moon phase, computed from real ephemeris data. Free, instant, no account required.',
};

export default function MoonCalculatorPage() {
  return <MoonCalculator />;
}
